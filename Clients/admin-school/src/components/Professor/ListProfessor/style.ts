import makeStyles from '@material-ui/core/styles/makeStyles';
import { tableHeader } from '../../../common/styles';

const useStyles = makeStyles({
  tableHeader: {
    ...tableHeader,
    alignContent: 'center',
  },
});

export default useStyles;
